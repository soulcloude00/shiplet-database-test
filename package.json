{
  "name": "shiplet-db-test-app",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny Shiplet demo app for testing a clean deployment.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node -e \"console.log('No build step required')\"",
    "start": "node server.js"
  },
  "dependencies": {}
}
