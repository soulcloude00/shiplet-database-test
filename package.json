{
  "name": "shiplet-db-test-app",
  "version": "1.0.0",
  "private": true,
  "description": "A tiny Postgres-backed app for testing Shiplet end to end.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node -e \"console.log('No build step required')\"",
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
